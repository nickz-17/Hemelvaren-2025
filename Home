<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Festival 2025</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; background-color: #222; color: white; }
        .container { padding: 50px; }
        .countdown { font-size: 2em; margin: 20px; }
        .button { display: inline-block; padding: 15px 25px; font-size: 1.2em; background: #ff5722; color: white; text-decoration: none; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Festival Zomer 2025</h1>
        <p>12 juli 2025 - Locatie: Unknown</p>
        <div class="countdown" id="countdown"></div>
        <a class="button" href="https://eventix.io" target="_blank">Koop Tickets</a>
    </div>

    <script>
        function updateCountdown() {
            const eventDate = new Date("July 12, 2025 00:00:00").getTime();
            const now = new Date().getTime();
            const timeLeft = eventDate - now;
            
            const days = Math.floor(timeLeft / (1000 * 60 * 60 * 24));
            const hours = Math.floor((timeLeft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((timeLeft % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((timeLeft % (1000 * 60)) / 1000);
            
            document.getElementById("countdown").innerHTML = `${days}d ${hours}u ${minutes}m ${seconds}s`;
            
            if (timeLeft < 0) {
                document.getElementById("countdown").innerHTML = "Het festival is begonnen!";
            }
        }
        setInterval(updateCountdown, 1000);
        updateCountdown();
    </script>
</body>
</html>
